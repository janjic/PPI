# This file sets the configuration of the 'User' entity.
#
# NOTE: this demo application splits the backend configuration into several
# small files for educational purposes. In your application you can put the
# entire backend configuration in just one file.
#
# The configuration options are explained in detail in the bundle documentation,
# which is published at https://github.com/javiereguiluz/EasyAdminBundle
easy_admin:
    entities:
        User:
            class: AppBundle\Entity\User
            list:
                title: 'Customers'
                actions:
                  - { name: 'edit', label: '', icon: 'edit' }
                  - { name: 'delete', label: '', icon: 'trash' }
                fields: ['id', 'isActive', 'username', 'email', 'roles', 'firstName', 'lastName', 'country', 'roles']
            form:
                fields:
                    - { type: 'group', label: 'Account Information', icon: 'user', css_class: 'col-sm-4' }
                    - id
                    - { property: 'username' }
                    - { property: 'plainPassword', label: 'Password', type: 'text', type_options: { required: false } }
                    - { property: 'firstName', type: 'text', type_options: { required: true } }
                    - { property: 'lastName', type: 'text', type_options: { required: true } }
                    - { property: 'isActive' }
                    - { property: 'roles', type: 'choice', type_options: { multiple: true, choices: { 'ROLE_USER': 'ROLE_USER', 'ROLE_ADMIN': 'ROLE_ADMIN' } } }
                    - { type: 'group', label: 'Contact Information', icon: 'envelope-o', css_class: 'col-sm-4' }
                    - { property: 'email', type: 'email'}
                    - { property: 'country', type: 'country', type_options: { required: false } }
                    - { property: 'city', type: 'text', type_options: { required: false } }
                    - { property: 'street', type: 'text', type_options: { required: false } }
                    - { type: 'group', label: 'Legal Information', icon: 'briefcase', css_class: 'col-sm-4' }
                    - { property: 'images', type: 'collection', type_options: { entry_type: 'AppBundle\Form\ImageType', by_reference: false }}
                    - { property: 'phoneNumbers'}
                    - { type: 'group', label: 'Projects included', icon: 'film', css_class: 'col-sm-12' }
                    - { property: 'projects', css_class: 'col-sm-12' }
            new:
                fields:
                    - '-id'
            edit:
                fields:
                    - { property: 'id', , type_options: { disabled: true }}
